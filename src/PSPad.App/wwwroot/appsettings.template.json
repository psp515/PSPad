{
  "Api": {
    "BaseAddress": "${API_BASE_ADDRESS}"
  },
  "Keycloak": {
    "Authority": "${KEYCLOAK_AUTHORITY}",
    "ClientId": "${KEYCLOAK_CLIENT_ID}",
    "PostLogoutRedirectUri": "${APP_BASE_ADDRESS}/authentication/logout-callback",
    "RedirectUri": "${APP_BASE_ADDRESS}/authentication/login-callback"
  }
}
